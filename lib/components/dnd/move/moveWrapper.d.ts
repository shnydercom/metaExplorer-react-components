import * as React from 'react';
import { IPosition } from '../interfaces';
export interface MoveWrapperProps extends IPosition {
}
export declare const MoveWrapper: React.FC<React.PropsWithChildren<MoveWrapperProps>>;
